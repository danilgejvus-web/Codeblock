# Codeblock

Проект запускается (обычно) посредством npm run dev в папке проекта, рекомендуется использовать Vite, а также может потребоваться установить Node.js, если этого не было сделано ранее.

Основным файлом для рендеринга является main.tsx
